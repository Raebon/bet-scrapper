{
  "name": "bet-scrapper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "start": "npm run compile && node ./build/src/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^21.2.1",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
